# app/ai/relevance.py
async def score_signal_for_all_orgs(db, source, category, severity, title,
                                     body, external_id, tags, external_url, published_at):
    """Stub — score incoming signal against all orgs and create Signal rows."""
    pass
